{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}